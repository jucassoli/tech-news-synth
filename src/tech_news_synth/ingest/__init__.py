"""Phase 4 ingest package — fetch, normalize, orchestrate.

Plan 04-01 ships the scaffolding + foundational modules (sources_config,
models, normalize, http, source_state repo). Plan 04-02 will add the
per-type fetchers and the orchestrator.
"""
