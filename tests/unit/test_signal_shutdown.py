"""RED STUB — Plan 02 Task 1 wires SIGTERM/SIGINT handlers.

Tests asserting scheduler.shutdown(wait=True) is called exactly once per
SIGTERM/SIGINT will be filled in by Plan 02.
"""

import pytest

pytest.skip("implemented in Plan 01-02", allow_module_level=True)
