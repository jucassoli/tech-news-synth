"""RED STUB — Plan 02 Task 1 implements tech_news_synth.scheduler.

Tests for INFRA-05 (BlockingScheduler PID 1, CronTrigger UTC, INTERVAL_HOURS
validator integration) will be filled in by Plan 02. This file exists now so
every requirement has a pre-declared test path (Nyquist continuity).
"""

import pytest

pytest.skip("implemented in Plan 01-02", allow_module_level=True)
